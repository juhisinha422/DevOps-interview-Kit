When we create a project involving AWS docker GitHub actions and terraform we should create infrastructure first using terraform and AWS first then use docker and GitHub actions for cicd right?

I would start by containerizing the application using Docker and ensuring it runs locally. Then I would introduce CI pipelines for building and testing the application. Once the application is stable, I would use Terraform modules to provision infrastructure efficiently. Finally, I would implement ArgoCD for GitOps-based continuous delivery to Kubernetes, ensuring automated and consistent deploymen
