Its mandatory question, today i will make you anser one question in interview out of all questions. without fail.

Q: how do you configure and manage secrets?
will store secrets in aws secret manager
configuration: you cant remember

will create secrets like username and password in secret manager and we keep some name for it like "test_secrets"

if you explain this interviewer think, you dont know, he is looking for these 
actually it starts from "test_secrets" creation
steps: 
create a policy in json(getsecretevalue:test_secrets with ARN) format, with the test_secrets ARN (amazon resourse name)
now test_secrets also a amazon resource, you want to share this resource with any other resources you need ARN url, policy name test_policy
****policy is created****
create role:
role must contain service whatever it is, suppose EC2 service you want use this secrets
now time to attach the policy under policy permissions, test_policy and attached, 
****policy attched****
what exactly this role do, role contains what actions you can do against secrets, like read, edit, write, delete these secrets, you are allowing required actions in role.
****role created****
now you have to attach this role to EC2
go to EC2 actions and get into security and modify i am role and attach this role.

now you can login EC2 now and check with aws get secret command, you will get username and password
